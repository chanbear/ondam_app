<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- debug/profile 빌드는 Flutter tool이 자동으로 병합해주지만 release는
         그렇지 않다 — 이게 없으면 release APK는 모든 네트워크 요청이
         실패한다(ONDAM 2.0 Phase 14에서 release 빌드 전용 재현 버그로 발견). -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />
    <!-- message_check(Phase 7): 최근 문자 조회 전용. RECEIVE_SMS/SEND_SMS는
         이 기능에 필요하지 않아 요청하지 않는다(최소 권한 원칙). -->
    <uses-permission android:name="android.permission.READ_SMS" />
    <!-- voice_assistant(Phase 10): 온디바이스 STT(speech_to_text) 전용. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!-- welfare_center/profile(Phase 25): "현재 위치로 자동 입력" 전용.
         행정구역 단위만 필요해(§9 개인정보 원칙) FINE이 아닌 COARSE만
         요청한다. -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <application
        android:label="온담"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- OAuth(구글/카카오) 로그인 완료 후 브라우저가 앱으로 돌아오는
                 딥링크. scheme/host는
                 auth_remote_datasource.dart의 `oauthRedirectUrl`과 반드시
                 동일해야 하고, Supabase 대시보드 Authentication → URL
                 Configuration의 Redirect URLs에도 같은 값을 등록해야 한다. -->
            <intent-filter android:autoVerify="false">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="io.ondam.senior" android:host="login-callback"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
