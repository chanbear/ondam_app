# UI 명세

> 기존 온담앱 UI를 참고하되 그대로 복제하지 않고, `.claude/rules/ui-design.md`의 토큰 체계(`AppColors`/`AppTextStyles`/`AppSpacing`/`AppRadius`)로 재해석한 새 UI 방향을 기록한다. 아직 분석 대상 자료가 제공되지 않아 비어 있다.

## 참고한 기존 화면

(자료 제공 후 작성 — 화면명 / 캡처 출처 / 참고한 요소)

## 개선 방향

(자료 제공 후 작성 — 기존 UI의 문제점과 새 방향)

## 토큰 매핑

(기존 값 → 가장 가까운 토큰 근사. 예: 기존 spacing 13px → `AppSpacing.sm`(8) 또는 `AppSpacing.md`(16), 근사 이유 기록)
