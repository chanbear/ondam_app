# Architecture (기능 반영)

> 저장소 전반의 아키텍처 규칙은 `.claude/rules/architecture.md`에 있다. 이 문서는 `feature-spec.md` 확정 이후, 실제로 추가되는 feature들이 기존 구조(`lib/app`, `lib/core`, `lib/features`)에 어떻게 적재되는지, 새 core 모듈(예: storage)이 필요한지를 기록한다. 아직 확정된 기능 명세가 없어 비어 있다.

## Feature 매핑

(자료 제공 및 feature-spec 확정 후 작성 — feature명 / 위치 / 의존하는 core 모듈)

## 신규 core 모듈 필요 여부

(예: 인증 토큰 저장을 위한 `core/storage` — 필요해지는 시점에 근거와 함께 기록)
