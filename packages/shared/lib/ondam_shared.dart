/// Intentionally empty. No cross-app utility has been needed yet — add real
/// exports here only when both the Senior and Guardian apps actually need
/// the same helper, not speculatively.
library;
